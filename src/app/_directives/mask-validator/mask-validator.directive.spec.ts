import { MaskValidatorDirective } from './mask-validator.directive';

describe('MaskValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MaskValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
